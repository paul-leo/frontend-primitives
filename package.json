{
  "name": "frontend-primitives-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Small, standalone TypeScript primitives for scoped resource lifecycles, instance pooling, and atomic multi-resource rollouts",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/paul-leo/frontend-primitives.git"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo build",
    "typecheck": "turbo typecheck",
    "test": "vitest run",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "turbo": "^2.3.3",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
